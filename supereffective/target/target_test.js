'use strict';

describe('myApp.target module', function() {

  beforeEach(module('myApp.target'));

  describe('target controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var targetCtrl = $controller('TargetCtrl');
      expect(targettrl).toBeDefined();
    }));

  });
});