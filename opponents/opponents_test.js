'use strict';

describe('pokemonFight.opponents module', function() {

  beforeEach(module('pokemonFight.opponents'));

  describe('opponents controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var opponentsCtrl = $controller('OpponentsCtrl');
      expect(opponentsCtrl).toBeDefined();
    }));

  });
});